## 1. 导出契约与构建接入

- [x] 1.1 为扩展 DLL 新增显式导出契约文件（如 `.def`），列出必需入口符号
- [x] 1.2 调整 `CMakeLists.txt`，将导出契约接入 `dbgx-mcp` 构建目标
- [x] 1.3 校验导出契约与 `src/dbgx-mcp.cpp` 当前导出入口保持一致

## 2. 导出验证门禁

- [x] 2.1 新增导出符号检查步骤，验证产物包含必需符号集合
- [x] 2.2 将导出检查接入本地验证流程（构建后检查或 CTest 任务）
- [x] 2.3 确保导出缺失时流程失败并给出可读错误信息

## 3. 加载文档与诊断

- [x] 3.1 更新 `README.md` 的 `.load` 示例，提供可直接复制的绝对路径写法
- [x] 3.2 在 `README.md` 补充加载失败排查步骤（路径存在性、依赖检查、导出检查）
- [x] 3.3 增加最小手工验收步骤，覆盖 WinDbg 加载成功与失败诊断入口

## 4. 验收与回归防护

- [x] 4.1 运行构建与测试，确认新增门禁不破坏现有 MCP 功能
- [x] 4.2 在 WinDbg 中执行最小加载验证，确认扩展可加载并完成初始化
- [x] 4.3 更新规格场景与验证步骤映射，保证新增需求可追踪

