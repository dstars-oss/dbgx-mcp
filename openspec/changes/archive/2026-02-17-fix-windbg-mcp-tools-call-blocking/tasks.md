## 1. 生命周期日志模型

- [x] 1.1 在 `src/windbg_mcp_extension.cpp` 设计并实现请求级日志上下文（关联键、阶段名、基础字段）封装
- [x] 1.2 为含 `id` 与缺失 `id` 的 JSON-RPC 请求统一生成可追踪关联标识，并保证同一请求内一致
- [x] 1.3 约定并落地最小阶段集合（如接收、执行开始、执行结束/失败、响应发送），确保每个阶段都有可读输出

## 2. 链路打点与摘要增强

- [x] 2.1 在请求处理链路增加阶段打点，确保 `tools/call` 在进入执行前输出阶段日志
- [x] 2.2 在执行返回路径输出结果阶段与耗时字段，区分成功/失败并保留错误摘要
- [x] 2.3 扩展 `src/mcp/io_echo.cpp` 摘要字段，补充工具名、阶段、结果状态、耗时等排障关键信息
- [x] 2.4 维持并验证敏感字段掩码与超长文本截断策略，禁止引入明文泄露回归

## 3. 测试覆盖

- [x] 3.1 更新 `tests/unit_tests.cpp`，覆盖阶段日志关键字段、成功链路与失败链路输出
- [x] 3.2 增加缺失 `id` 场景测试，验证本地关联标识存在且同请求阶段一致
- [x] 3.3 增加阻塞可定位性测试（至少验证执行前阶段日志先于响应阶段出现）
- [x] 3.4 运行 `ctest --test-dir build -C Debug --output-on-failure` 并修复新增失败

## 4. 文档与验收

- [x] 4.1 更新 `README.md` 的诊断章节，加入新版日志字段说明与示例
- [x] 4.2 更新 `README.zh-CN.md`，保持与英文文档的诊断结构一致
- [x] 4.3 记录最小人工验收步骤：复现 `tools/call` 正常/失败/阻塞三类场景并核对 WinDbg 日志可读性
