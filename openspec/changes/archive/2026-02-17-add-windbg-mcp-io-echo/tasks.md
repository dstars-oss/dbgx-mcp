## 1. 回显内容建模与安全规则

- [x] 1.1 在 MCP 处理链路新增回显格式化辅助逻辑，统一生成请求摘要与响应摘要文本
- [x] 1.2 为摘要逻辑实现长度截断与 `...(truncated)` 标记，确保超长内容可控
- [x] 1.3 为敏感字段（如 Authorization）实现掩码规则，禁止输出原始敏感值

## 2. 请求/响应回显接入

- [x] 2.1 在 `/mcp` 请求入口接入请求摘要输出，包含方法、路径及可解析的 JSON-RPC 元信息
- [x] 2.2 在请求处理结束路径接入响应摘要输出，覆盖成功响应与错误响应
- [x] 2.3 确保回显逻辑失败时不影响原有 HTTP/JSON-RPC 返回语义

## 3. 测试与验证

- [x] 3.1 为摘要、截断与掩码规则补充单元测试用例
- [x] 3.2 为成功/失败请求路径补充行为测试或可追踪映射，验证均有 I/O 回显
- [x] 3.3 在 WinDbg 会话中执行最小手工验证，确认窗口可见输入/输出回显
